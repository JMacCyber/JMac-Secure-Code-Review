"""JMac Secure Code Review (JSCR).

A security-first code review engine. Deterministic policy decides what is
possible; the model only decides what to ask for.

The core package depends on the Python standard library only. External
scanners and model providers are optional and disabled by default.
"""

__version__ = "0.1.0"
__all__ = ["__version__"]
